manifest: ./lzc-manifest.yml

buildscript: cd app/backend && npm install --production && cd ../..
contentdir: ./app

pkgout: ./

icon: ./lzc-icon.png


devshell:
  routes:
    - /=http://192.168.1.161:3000
    - /api/=http://192.168.1.161:3000/api/
  dependencies:
    - nodejs
    - npm
  setupscript: |
    ls
    # cd ./app/web
    # npm install
    # npm run build
    # cd ./..
    # npm ./backend
    # npm install
    # npm run start

